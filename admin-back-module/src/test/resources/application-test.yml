spring:
  datasource:
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: validate

logging:
  level:
    root: info
#    org.hibernate.orm.jdbc.bind: TRACE
#    org.springframework.jdbc.core: TRACE
app:
  image:
    products:
      bucket: /tmp/images/products
      prefix-file-name: "product_"
  init:
    default-admin:
      email: testadmin@example.com
      password: testpassword